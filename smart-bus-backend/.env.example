DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=smartbus
PORT=5000
# Admin seeding (optional) - used when running `npm run init-db` with these env vars set
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=AdminPass123
# JWT secret - change in production
JWT_SECRET=change-this-secret
